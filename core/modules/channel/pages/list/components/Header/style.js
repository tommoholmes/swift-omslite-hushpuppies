import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    headerContainer: {
        paddingBottom: '32px',
    },
    title: {
        display: 'inline',
    },
    buttonAdd: {
        display: 'inline',
        float: 'right',
    },
}));

export default useStyles;
