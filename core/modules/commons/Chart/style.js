import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    line: {
        boxShadow: 'none',
    },
    bar: {
        boxShadow: 'none',
    },
    none: {
        display: 'none',
    },
}));

export default useStyles;
