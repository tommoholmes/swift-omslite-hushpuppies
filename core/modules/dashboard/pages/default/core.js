import Layout from '@layout';

const Core = (props) => {
    const {
        Content,
    } = props;

    return (
        <Layout>
            <Content />
        </Layout>
    );
};

export default Core;
