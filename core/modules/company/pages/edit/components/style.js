import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    container: {
        padding: '16px',
    },
    formField: {
        padding: 0,
        paddingBottom: 16,
    },
}));

export default useStyles;
