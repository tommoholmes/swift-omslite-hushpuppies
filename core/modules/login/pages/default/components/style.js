import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    loginContainer: {
        margin: 36,
    },
    textTitle: {
        padding: '12px 6px',
    },
    formField: {
        padding: 16,
    },
}));

export default useStyles;
