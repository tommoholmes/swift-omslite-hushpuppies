import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    loginContainer: {
        margin: 36,
    },
    formField: {
        padding: 16,
    },
}));

export default useStyles;
